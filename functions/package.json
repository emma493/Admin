{
  "name": "shortxx-functions",
  "description": "Server-side HLS transcode pipeline for shortxx (2nd-gen Cloud Functions, triggered on Firestore writes to videos/{videoId}).",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "echo \"(no linter configured)\"",
    "deploy": "firebase deploy --only functions:transcodeVideo",
    "logs": "firebase functions:log --only transcodeVideo"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.0",
    "fluent-ffmpeg": "^2.1.3",
    "ffmpeg-static": "^5.2.0"
  }
}
